class EntityLoader {

    static load(type, entity) {
        switch (type) {
            case "creature":
                break;
            case "email":
                break;
            default:
                break;
        }
    }
}

export default EntityLoader